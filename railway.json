{
    "build": {
        "command": "pip install -r requirements.txt && chmod +x scripts/database_seeder.py"
    },
    "deploy": {
        "postdeploy": "flask db upgrade && sleep 15 && python -m scripts.database_seeder"
    }
}