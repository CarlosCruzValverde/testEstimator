{
    "build": {
        "command": "pip install -r requirements.txt && chmod +x scripts/database_seeder.py"
    },
    "deploy": {
        "postdeploy": "flask db upgrade && sleep 60 && python scripts/database_seeder.py"
    }
}